# Value stream map (activity 247)

Fill `streamlit/data/value_stream.yaml`. Prove each remaining view with `scripts/create_athena_views.py` / `sql/proof_views.sql`. Paste QueryExecutionId onto the YAML row.

Default stages: Biz → `github_issues`; Dev → `github_pull_requests` + commits; Test/Sec/Ops → `github_workflow_runs`. Skill **Value Stream** → Recipe step 2 and 4.
